# tiny-intset

Compact ASCII serialization experiments for small integer sets and multisets.

The package is currently reserved for an upcoming implementation.

Planned scope:

- serialize integer arrays in the range 1..300
- deserialize back into arrays
- support browser and Node.js usage
- provide test cases with compression ratios
